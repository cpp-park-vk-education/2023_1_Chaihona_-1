#pragma once
#include "match.hpp"
#include <string>
#include <vector>

class MatchRequests {
private:
    unsigned firstId;
    unsigned secondId;
    bool match;
public:
    MatchRequests() {};
    bool matchResult() {};
    unsigned insert() {};
};